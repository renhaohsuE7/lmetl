services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    mem_limit: 2g  # docx parse + LLM extraction; never run containers unbounded
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./configs:/app/configs
    env_file:
      - .env
    environment:
      - LMETL_CONFIG=configs/base.yaml
    command: >
      uv run pwetl --config configs/base.yaml

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    mem_limit: 2g
    volumes:
      - ./tests:/app/tests
      - ./src:/app/src
      - ./configs:/app/configs
      - ./data:/app/data
      - ./output:/app/output
    command: uv run pytest tests/ -v
